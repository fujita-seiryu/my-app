# 移動ドピッチトレーナー 開発設計書 (v20)

## 1. 背景・目的

- 御詠歌を行っており、**移動ド**（相対音感）の考え方には慣れている
- 一方で**楽器演奏の経験がなく、コード（和音記号）を見ても理解できない**
- Roland VT-12（ボーカル・トレーナー）の実機を所有・使用しているが、練習曲・録音再生などの機能は不要
- VT-12使用時は、自作の**円形シール**（ド/レ/ミ/ミ#/ファ/ソ/ラ/ラ#/シの階名を記載）を本体の上に貼り、基準音が変わるたびに貼り替えて使っていた
- **これをデジタルで再現したい**：基準音を選ぶだけでシールの貼り替えなしに同じ表示が得られるようにする
- メトロノームも欲しい（拍子の強弱区別あり）
- **設計の根本方針**：本アプリはVT-12という音楽専門メーカー（Roland）が実売した専用ツールへのリスペクトを出発点として作り始めた。円形メーターの配置は、数学的な正確さ（半音距離の比例配置）よりも、**実機シールに忠実であること**を優先する

## 2. 想定ユーザー・利用環境

- 開発者本人（1名）。楽器演奏・五線譜の知識なし、移動ド唱法には慣れている
- **スマホのブラウザ**で使用（VT-12のような縦型・手持ちサイズのUI）
- VT-12実機と見比べながら開発・調整する

## 3. 必須機能（スコープ内）

| # | 機能 | 内容 |
|---|------|------|
| 1 | 基準音（音名）選択 | イロハニホヘトの自然音＋#（例：ヘ#）＝計12種類から選択 |
| 2 | オクターブ層選択 | 基準／ー1／ー2 の3段階（4段目の「＋」は実使用がないため廃止） |
| 3 | マイク入力によるリアルタイムピッチ検出 | ブラウザのマイク入力から歌声の基本周波数(f0)を検出 |
| 4 | 移動ド変換・表示 | 検出した音を、選択した基準音からの相対位置に変換し、実機VT-12のシール配置を再現した9分割の円形メーターで表示。「ミ短」「ラ短」の2区画は御詠歌の転調に対応する固定区画として常設 |
| 5 | 音程のズレ表示 | ジャストな音程からのズレ（セント単位）を円形メーターの光り方・色で表現 |
| 6 | メトロノーム | BPM設定（30〜90、デフォルト50）、開始/停止、拍子は常に2拍子固定。BPMの数値（表拍の間隔）は変えずに各表拍の間に裏拍を1つ挟むため、通常時は表拍2回＋裏拍2回＝計4回、倍速ボタンON時は表拍4回＋裏拍4回＝計8回鳴る。通常速度（倍速OFF）では表拍の音はすべて同じ（ピンポンピンポン）で、倍速ONのときのみ1・3打目（元の2拍子の頭）にアクセントが付く（ピンパンピンパン）。裏拍はさらに控えめな音で表拍と区別する。画面に表示する丸（拍インジケーター）は表拍の数だけ（通常2個・倍速4個）とし、表拍・裏拍を含むすべての音のタイミングでドットが順番に1つずつ光る。アクセントのドットは必ず表拍で黄色に、非アクセントのドットは必ず裏拍で白に光る |
| 7 | となえ練習 | ボタンを押すとピッチ音（移動ドの相対音程で構成した節）が鳴り、それを聞きながら一緒に唱える練習用の機能。全8パターン（１・１／２・２／２・１・１／１・１・２／３・１／１・３／１・１・１・１／１・２・１）を実装済み。パターン選択ボタンは押しても即再生せず「選択」状態になるのみで、別途「長音階／短音階」切り替えボタンと「START」ボタンを用意し、選択中のパターン・音階で実際に再生を開始/停止する。どのパターンも再生前に2拍のカウントイン（メトロノームと同じ音色）を鳴らす（詳細は12章） |

## 4. スコープ外（あえて実装しない機能）

- 内蔵練習曲・トレーニングコンテンツ
- 録音・再生・レビュー機能
- 2声同時検出（コーラス機能）
- 教則本連携・ウォームアップモード

## 5. 基準音・オクターブ層の選択UI（2軸分離方式）

以前は「基準音×オクターブ」を1つの選択肢（例：ーーハ、ーハ、ハ、＋ハ…）としてまとめて考えていたが、7音×#の有無×オクターブ段階＝最大42通りとなり選びにくいため、**独立した選択UI**に分離する。

（一時期、音名とオクターブ層の間に「調（長調/短調）」選択を追加していたが、御詠歌は曲中で転調することがあり、曲全体を長調/短調のどちらかに固定するトグル方式は実態に合わないため撤去した。詳細は6-2参照）

### 5-1. 基準音（音名）選択：12種類
```
イ・イ#・ロ・ハ・ハ#・ニ・ニ#・ホ・ヘ・ヘ#・ト・ト#
```
（ホ-ヘ間、ロ-ハ間はもともと半音のため「ホ#」「ロ#」は存在しない＝ピアノの黒鍵5つに対応する数と一致）

### 5-2. オクターブ層選択：3段階
| 表記 | 意味 | 目安 |
|------|------|------|
| 基準 | 御詠歌の楽譜上の基準（記譜音） | 女性キーよりやや高め寄り |
| ー1 | 基準より1オクターブ下 | おおむね女性キー相当 |
| ー2 | 基準より2オクターブ下 | おおむね男性キー相当 |

「＋」（基準より1オクターブ上）は実際の声域で使用しないため廃止。

**プレビュー再生時の低域対策**：「ー2」を選択している状態でリング区画をタップしてプレビュー再生すると、iPhone等の内蔵スピーカーでは超低域が再生できずほぼ聞こえない問題があった。そのため、プレビュー再生（タップ時の音出し）に限り「ー2」のときだけ「ー1」相当の周波数で鳴らすよう変更した。ピッチ検出・判定に使う基準周波数（オクターブ層どおり「ー2」）自体は変更していない。実際に耳で確認し、歌う上での違和感がないことを確認済み。

### 5-3. 補足：男女ワンタッチ切り替え（検討中の追加案）
昔使っていた「コンダクター」という機械にあった、ボタン一つで基準音を1オクターブ下げて男性キー用に切り替える機能を、ショートカットボタンとして実装することも検討。実装が固まった段階で正式に採用可否を決める。

## 6. 円形メーター表示ロジック（実際のシールを再現）

### 6-1. 表示する9区画（実機同様12区画の枠に配置、固定・唯一の表示パターン）
実機VT-12は12個の固定ピッチ位置（半音ごと）を持つため、円形メーターも**12区画（30°ずつ）を土台**とし、そのうち9区画にラベルを配置する。残り3区画（実際に使わない半音の位置）は空白のまま残す。

```
反時計回り（上行方向）: ド → レ → ミ → ミ短 → ファ → ソ → ラ → ラ短 → シ → (ド)
```
円周上の配置（画像から再現、時計回り）：
```
ファ → (空白) → ソ → ラ短 → ラ → (空白) → シ → ド → (空白) → レ → ミ短 → ミ → (12時に戻る)
```
- 12時の位置：ミ（左）／ファ（右）が隣接
- 空白マスは**ド-レ間・ファ-ソ間・ラ-シ間**の3箇所（実機で使わない半音に相当）

### 6-2. ミ短・ラ短の扱い（転調対応、トグル方式の撤去経緯）
当初は「長調/短調」を切り替えるセレクタを実装し、短調を選ぶと「ミ」「ラ」の鳴る周波数がそれぞれレ#相当・ソ#相当に切り替わる仕様だった。しかし御詠歌は曲の途中で転調することがあり、**曲全体を長調/短調のどちらかに固定する**トグル方式は実態に合わないことが判明したため撤去した。

代わりに、旧「ミ#」「ラ#」の2区画を、転調時に使う音専用の**常設区画**として恒久的に置き換えた。ラベル（階名の呼び方）を変えるだけで、リング上の位置（実機シール配置）は変更していない。

| 旧ラベル | 旧semitone | 新ラベル | 新semitone | 備考 |
|------|------|------|------|------|
| ミ# | 5（ファと同じ） | ミ短 | 3 | レ#相当。実際に御詠歌で使うコンダクター実機で周波数を確認済み |
| ラ# | 10 | ラ短 | 8 | ソ#相当。同上 |

この変更により、9区画すべてが異なる周波数（semitone）を持つようになり、以前あった「ミ#とファの異名同音による同時ハイライト」は発生しなくなった。曲中で長調・短調どちらの音が必要になっても、セレクタの切り替えなしにいつでも該当区画をタップ・検出できる。

### 6-3. オクターブ違いの表示（実機検証で判明）
選択した基準音・オクターブ層から算出した「基準オクターブ」に対して、実際に検出された音が別のオクターブだった場合でも、移動ドの度数（階名）としては正しく同じ階名で表示されるべき。ただし、どのオクターブで鳴っているかが分かるよう、色を変えて区別する：

| 状態 | 表示色（例） |
|------|------|
| 基準オクターブと一致 | 赤 |
| 基準オクターブより下 | 黄 |
| 基準オクターブより上 | 青（実使用は少ない想定） |

計算式：検出周波数からセント差を算出し、最も近い階名区画を求める。オクターブ差は `round(12 × log2(検出Hz ÷ 基準ドHz) / 12)` 等で算出し、表示色に反映する。

## 7. 音域

- ピッチ検出自体は基準オクターブの前後1オクターブ程度（計3オクターブ弱）をカバーできるようにし、6-3のオクターブ判定ロジックで正しく色分けする
- 範囲外の周波数（ノイズ・倍音誤検出）は無視する

## 8. デザイン方針（VT-12シール踏襲・スマホ縦型）

```
┌───────────────────┐
│ 音名:[ハ▼] オクターブ:[基準▼]        │
│                             │
│        ミ  │  ファ           │
│    ミ短     (空白)   ソ       │  ← 実機同様12区画の枠
│    レ    ╭────╮   ラ短        │     （9区画にラベル、3区画は空白）
│  (空白)    │現在│   (空白)     │
│    ド    ╰────╯   ラ         │
│        シ                   │
│                             │
├───────────────────┤
│ ♪ BPM[ 50 ]       [ 倍速 ]   │
│      [ ● ○ ]（表拍のみ表示）│
│      [ 開始 / 停止 ]         │
└───────────────────┘
```
- 縦長（スマホ画面向き）の1画面完結レイアウト
- 音名・オクターブ層は横並びの2つの独立したセレクタ（調セレクタは撤去済み）
- 円周は実機同様12区画（30°ずつ）を土台とし、9区画にラベルを配置。残り3区画（ド-レ間・ファ-ソ間・ラ-シ間）は空白
- 現在検出中の音の区画を光らせる。オクターブ差は色で表現（6-3参照）
- 拍インジケーターのドットは表拍の数だけ表示（通常時2個・倍速時4個）。表拍・裏拍を含むすべての音のタイミングでドットが順番に1つずつ光る。アクセントのドットは必ず表拍で黄色に、非アクセントのドットは必ず裏拍で白に光る（9技術方針参照）

## 9. 技術方針

- **形態**：ブラウザで動く単一Webアプリ（index.html / style.css / script.js）、サーバー不要
- **マイク入力**：`getUserMedia`
- **ピッチ検出**：Web Audio API の `AnalyserNode` ＋自己相関法（autocorrelation）
- **メトロノーム**：`AudioContext.currentTime` 基準の lookahead スケジューリング。拍子は常に2拍子固定。実際の御詠歌での使用範囲に合わせ、拍速（BPM＝表拍の間隔）は30〜90（デフォルト50）に制限。「倍速」ボタンで表拍の速さを2倍に切り替えられる（切り替え時は小節の頭にリセット）。さらに各表拍の間に裏拍を1つ挟んで鳴らす（裏拍機能。詳細は後述）
- **フレームワーク**：Vanilla JSで十分
- **スマホ対応**：viewportメタタグ、タッチ操作前提のUIサイズ、ポートレート固定推奨

**倍速機能**：「倍速」ボタンをONにすると、2拍子の表拍をそのまま2倍の速さで刻む。ONにすると表拍が1・2・3・4と4回打たれ、そのうち1・3打目（＝元の2拍子の頭）にアクセントが付く（2拍子を単純に倍速再生した自然な形）。倍速ON時はBPM表示・拍速スライダーの数値は変えず、内部の表拍間隔のみ半分にする。切り替え時は小節の頭にリセットする。

（以前は「2拍子／4拍子」を切り替える拍子ボタンも別途あったが、倍速ボタン1つで同じ表現ができ、ボタンが増えて分かりにくいため撤去した。拍子（beatsPerBar）は常に2で固定し、倍速のON/OFFのみで表拍の「タンタン」を通常速度で鳴らすか2倍速で鳴らすかを切り替える。）

**裏拍機能**：BPMの数値・意味（表拍の間隔）は変えずに、各表拍と表拍のちょうど中間に裏拍（オフビート）を1つ挟んで鳴らす。これにより実際に鳴る音の数は表拍数の2倍になり、通常時は表拍2回＋裏拍2回＝計4回（タン・ッ・タン・ッ）、倍速ON時は表拍4回＋裏拍4回＝計8回（タン・ッ・タン・ッ・タン・ッ・タン・ッ）鳴る。裏拍は700Hz・音量0.2とし、表拍よりさらに控えめで低い音にして耳で区別できるようにした。

**表拍の強弱（通常速度と倍速の違い）**：通常速度（倍速OFF）では、表拍2回はどちらも同じ音（1500Hz・音量0.6、「ピン」）にし、強拍/弱拍の区別をつけない（＝「ピンポンピンポン」）。倍速ONのときのみ、表拍4回のうち1・3打目（強拍）を1500Hz・音量0.6、2・4打目（弱拍）を1000Hz・音量0.35として区別する（＝「ピンパンピンパン」）。

**拍インジケーターのドット表示**：画面に表示する丸（ドット）の数は表拍の数だけとし、通常時2個・倍速時4個で固定する（裏拍のぶんはドットを増やさない）。速度（BPM・裏拍を含む鳴る回数）自体は変更せず、画面上の見た目だけをシンプルにした。

**点滅の割り当て方**：表拍・裏拍を含むすべての音のタイミング（subIndex）を、ドットの並びに先頭から順番に１つずつ（`subIndex % ドット数`の余り）割り当てて光らせる。ドット数（＝表拍数）は常に偶数（2または4）のため、この割り当ては必ず「偶数番目のドット＝表拍専用」「奇数番目のドット＝裏拍専用」に固定される。1・3個目（元の2拍子の頭）などアクセントの付くドットは偶数番目にあたるため必ず表拍のときだけ黄色に、それ以外の非アクセントのドットは奇数番目にあたるため必ず裏拍のときだけ白に光る。色分けはCSSの`beat-dot.accent.on`／`beat-dot.on`がドットの位置だけで自動的に行う。

## 10. 音名対応（イロハ ⇔ 移動ド 変換ロジック）

```
イ = A (ラ)　ロ = B (シ)　ハ = C (ド)　ニ = D (レ)
ホ = E (ミ)　ヘ = F (ファ)　ト = G (ソ)
```
選んだ音名を「ド」として、検出周波数との半音差から、6章の円形メーターの区画を点灯させる。オクターブ層（基準/ー1/ー2）は、その「ド」の絶対的な高さ（何Hzか）を決定する役割。

## 11. Claude Codeでの実装ステップ（タスク分解・現状）

1. プロジェクト雛形作成（index.html / style.css / script.js）、viewport設定 ✅
2. マイク入力取得・音量メーターで疎通確認 ✅
3. ピッチ検出アルゴリズム実装（自己相関法） ✅
4. 音名選択UI＋オクターブ層選択UI実装、半音差計算ロジック実装 ✅（実装中に「＋」廃止・2軸分離へ変更）
5. 円形メーターUI実装（実機シール踏襲の9分割固定配置） ✅
6. 半音距離に比例した8区画配置へ再設計（ドを12時、半音=30°、ミ#/ファは1区画に統合） ✅→その後7で撤回
7. **周波数比例配置を撤回し、9分割固定シール配置に再度戻す** ✅（根拠：本アプリはVT-12という音楽専門メーカーの実売ツールへのリスペクトを出発点として作られており、数学的正確さより実機シールへの忠実さを優先するという設計方針を確定）
8. オクターブ表記を「無印/ー/ーー」から「基準/ー1/ー2」に変更 ✅
9. 円形メーターを実機同様12区画（30°）の枠に修正し、ド-レ間・ファ-ソ間・ラ-シ間の3区画を空白化 ✅
10. 長調/短調切り替えセレクタを追加（音名とオクターブ層の間） ✅→のちに転調に対応するため撤去し、ミ短/ラ短の固定区画に変更（ステップ11）
11. **長調/短調トグルを撤去し、ミ#→ミ短（レ#相当）、ラ#→ラ短（ソ#相当）に変更** ✅（根拠：御詠歌は曲中で転調することがあり、曲全体を長調/短調どちらかに固定するトグル方式は実態に合わないため。6-2参照）
12. メトロノーム実装（lookaheadスケジューリング＋拍子ごとの強拍アクセント） ✅
13. **拍速の範囲を実使用に合わせて30〜70（デフォルト50）に変更し、「2拍子」「4拍子」を切り替える専用ボタンを追加** ✅→のちにステップ19で拍子ボタン自体を撤去し倍速ボタンに統一（根拠：実際の御詠歌では拍速が1分間30〜70の範囲を超えることがなく、拍子も「タンタン」（2拍子）と「タンタンタンタン」（4拍子）の切り替えで足りるため）
14. **プレビュー再生が「ー2」オクターブで聞こえない問題を修正**：プレビュー再生時のみ「ー2」を「ー1」相当の周波数に差し替え ✅（根拠：iPhoneの内蔵スピーカーが超低域を再生できないため。ピッチ検出・判定側の基準周波数は変更なし。5-2参照）
15. **拍速の上限を70→90に拡張** ✅（根拠：実使用の範囲を再確認し、上限を引き上げる方が実態に合うと判明したため）
16. **「倍速」ボタンを追加**：拍子パターン（2拍子／4拍子）はそのまま、2倍の速さで刻めるようにした ✅→ステップ19で拍子ボタン自体を撤去（根拠：「2拍子」の倍速で「タンタンタンタン」と4回鳴らしたいという要望。アクセントは1・3拍目に付く。9技術方針参照）
17. スマホ縦型レイアウトの全体スタイリング・調整
18. 実際の声・実機VT-12＋シールと比較しながら動作確認、検出感度やUI反応速度を微調整（進行中：実測でオクターブ表示ロジックを検証済み）
19. **「2拍子／4拍子」の拍子切り替えボタンを撤去し、「倍速」ボタンのみに統一** ✅（根拠：拍子は常に2拍子固定とし、倍速ON/OFFだけで「タンタン」⇔「タンタンタンタン（1・3拍目アクセント）」を切り替えれば十分で、ボタンが2つあると分かりにくいため。9技術方針参照）
20. **メトロノームに裏拍を追加**：BPMの数値・意味は変えずに各表拍の間に裏拍を1つ挟み、通常時・倍速時とも音数を2倍にした（通常4回・倍速8回） ✅（根拠：「今の基本と倍速、共に倍の回数を鳴らしてほしい」「bpmの数値に対して裏拍を入れてください」という要望。音色は強拍1500Hz/弱拍1000Hz/裏拍700Hzの3段階で区別。9技術方針参照）
21. **拍インジケーターのドット数を表拍のみに変更**：速度・鳴る音の回数（裏拍含む）はステップ20のまま変更せず、画面に表示する丸の数だけを表拍の数（通常2個・倍速4個）に戻した ✅（根拠：「現在は通常速度でタンが4回表示、倍速でタンが8回表示されています。速度は今のままで良いので、画面に表示される丸の数を通常速度で２個、倍速で４個に変更しなさい」という要望。裏拍は音のみで鳴らしドットは光らせない。9技術方針参照）→のちにステップ22で裏拍のタイミングでも同じドットを控えめに光らせるよう修正
22. **拍インジケーターの点滅を音に同期するよう修正**：ステップ21で裏拍を画面に表示しなくした結果、音（表拍・裏拍とも計4回/8回）と点滅（表拍のみ2回/4回）のタイミングが合わなくなっていた問題を修正。ドットの数は表拍のみ（通常2個・倍速4個）のまま変えず、各ドットが自分の表拍で明るく、直後の裏拍で控えめに光るようにし、点滅の回数・タイミングを実際に鳴る音と一致させた ✅（根拠：「音と点滅があっていません。音に合わせて点滅するように修正してください。」という指摘。9技術方針参照）→のちにステップ23でこの「音に完全同期」方針を撤回し、表拍のみ1回点灯に戻した
23. **拍インジケーターの点滅をステップ21以前の「表拍のみ1回点灯」に戻し、非アクセントの色を緑→白に変更**：ステップ22で導入した「裏拍のタイミングでも控えめに光らせる」仕組みを撤回。点滅は表拍のタイミングのみ・1回だけとし、アクセント（1・3打目）は黄色、非アクセントは緑ではなく白で点灯するようにした。裏拍は従来どおり音のみで鳴り、点滅はつけない ✅（根拠：「現在は①で黄色点灯、白色点灯のち、②で緑色点灯、白色点灯の順番で繰り返しています。私のイメージでは、①で黄色点灯、②で白色点灯、これを繰り返すだけです」「倍速の場合、aで黄色点灯1回、bで白色点灯1回、cで黄色点灯1回、dで白色点灯1回の順番で繰り返すだけです」という指摘。9技術方針参照）→のちにステップ24で、裏拍でも別のドットを光らせるよう再度修正
24. **拍インジケーターを表拍・裏拍とも交互に点灯するよう修正**：ステップ23では裏拍に点滅がなかったが、表拍・裏拍を含むすべての音のタイミングでドットを順番に1つずつ光らせるようにした（`subIndex % ドット数`で割り当て）。ドット数は常に偶数のため、この割り当ては必ず「アクセントのドット＝表拍だけで黄色」「非アクセントのドット＝裏拍だけで白」に固定される。通常速度は①黄→②白→①黄→②白…、倍速はa黄→b白→c黄→d白→a黄→b白→c黄→d白…と交互に繰り返す ✅（根拠：「私のイメージでは、①で表拍黄色点灯、②で裏拍白色点灯、①で表拍黄色点灯、②で裏拍白色点灯、これを繰り返すだけです」「倍速の場合、aで表拍黄色点灯1回、bで裏拍白色点灯1回、cで表拍黄色点灯1回、dで裏拍白色点灯1回…の順番で繰り返すだけです」という指摘。9技術方針参照）
25. **通常速度の表拍の音を強拍/弱拍の区別なしに統一**：倍速はそのまま（1・3打目が強拍・2・4打目が弱拍で「ピンパンピンパン」）維持しつつ、通常速度（倍速OFF）のみ表拍2回をどちらも同じ音（1500Hz・音量0.6）にし「ピンポンピンポン」にした。裏拍・点滅ロジックは変更なし ✅（根拠：「通常、倍速ともに、拍速の音がピンポンパンポンと表拍にピンとパンというような違いがあります。倍速は今のままでいいのですが、通常をピンポンピンポンというように表拍の音を同じものにしてください。」という要望。9技術方針参照）
26. **「となえ練習」機能を新規追加し、パターン1を実装**：ピッチ音を聞きながら一緒に唱える練習用の新機能。8パターン分のボタン枠を先に用意し、内容が決まったパターンから1つずつ実装していく方針。パターン1（拍速45、ド・レ・ミ・ソ・ラ・ド↑・レ↑・ミ↑・休・ミ↑・ド↑・ラ・ソ・ミ・レ・ドの16拍）を実装し、各拍いっぱいまで音を伸ばすレガート再生とし、再生中は音名リングの対応区画に緑の光の輪（`chant-target`）を表示して今どの音が鳴っているかを示す仕様にした ✅（根拠：「ボタンを押すと、ピッチ音が再生され、それを聞きながら一緒に唱える練習です。全部で8パターン用意します。まずは1パターン目…」という要望。詳細は12章参照）
27. **パターン1を17拍構成に修正し、パターン2〜8を実装。カウントイン・長短音階切り替え・選択式UIを追加**：パターン1を「ド→レ→ミ→ソ→ラ→ド↑→レ↑→ミ↑→休→ミ↑→レ↑→ド↑→ラ→ソ→ミ→レ→ド」の17拍（対称な上行下行）に修正し「１・１」と命名。パターン2〜8（２・２／２・１・１／１・１・２／３・１／１・３／１・１・１・１／１・２・１）を、1拍を4単位に分割するリズム表記（`ー`＝直前の音を1単位伸ばす、`↑`＝1オクターブ上）に基づき実装。あわせて、どのパターンも本編の前に2拍のカウントイン（メトロノームと同音色の表拍ピン・裏拍ポン）を鳴らす仕様、パターン選択ボタンを「押すと選択状態になるだけ（即再生しない）」方式に変更、長音階／短音階の切り替えボタン、実際に再生を開始/停止する専用のSTARTボタンを追加した ✅（根拠：「ド→レ→ミ→ソ→ラ→ド↑→レ↑→ミ↑→休→ミ↑→レ↑→ド↑→ラ→ソ→ミ→レ→ドに修正。パターン１の名称「１・１」とする。どのパターンでも音を鳴らし始める前に２拍テンポを示すためにメトロノームの音を鳴らすことにする。表拍ピン裏拍ポン表拍ピン裏拍ポンと鳴らしてから最初の一拍目の音を鳴らし始める。（中略：パターン2〜8それぞれの名称・リズム表記・音の並びを個別に指定）…パターンは以上の8パターンであるが、これらは長音階である。8パターンの選択に加えて、長音階と短音階の切り替えボタンを追加し、STARTというボタンを加える。短音階の場合、上記８パターんのうち、「ミ」はレ＃、「ラ」はソ＃を鳴らすこととする。」という要望。詳細は12章参照）

## 12. となえ練習の仕様

新機能「となえ練習」は、選択したパターン・音階でピッチ音（移動ドの相対音程で構成した節）を再生し、それを聞きながら一緒に唱える練習を行うための機能。全8パターンを実装済み。

### 12-1. UI構成
- 画面下部に「となえ練習」セクションを新設し、`CHANT_PATTERNS`の各パターン名（１・１〜１・２・１）をラベルにしたボタンをスクリプトが動的に2列グリッドで生成する
- パターンボタンは押しても即再生せず、選択状態になるだけ（`.chant-btn.selected`、黄色でハイライト）。選択を切り替えても再生中の音には影響しない
- 併せて「長音階／短音階」切り替えボタン（`chantScaleToggle`）と、選択中のパターン・音階で実際に再生を開始/停止する「START」ボタン（`chantStartBtn`）を配置する。STARTを押すと表示が「STOP」に変わり、もう一度押すと途中でも停止できる
- 再生中はパターン選択ボタン・音階切り替えボタンをすべて非活性（disabled）にし、再生の途中でパターンや音階が切り替わらないようにする

### 12-2. 再生方式（レガート・拍のサブディビジョン）
- 1拍は4つの「単位」（16分音符相当×4）に分割できる。パターンによっては1拍の中に複数の音を配置する（例：「２・２」は1拍を2:2に、「１・１・２」は1拍を1:1:2に分割）。休止は1拍まるごとの無音として表現する
- 各音は、自分に割り当てられた単位数ぶんの長さを、次の音が始まる直前までほぼいっぱいに伸ばして鳴らす「レガート」方式を維持する
- 音同士の間にはポップ音（プツッというノイズ）防止のためのごく短い無音・フェード区間のみを設ける
- 曲全体の長さが短いため、メトロノームのようなlookahead（先読み）スケジューリング方式ではなく、再生開始時に全ての音（カウントイン含む）をまとめて`AudioContext`にスケジューリングする方式を維持する

### 12-3. カウントイン
- どのパターンでも、本編の音を鳴らし始める前に2拍ぶんのカウントインを鳴らす
- 表拍ピン（1500Hz・音量0.6）→裏拍ポン（700Hz・音量0.2）→表拍ピン→裏拍ポンの順で、メトロノームの通常速度（8章参照）と全く同じ音色・音量で鳴らし、その直後にパターン本編の最初の拍を鳴らし始める

### 12-4. 音程・周波数の計算（長音階／短音階対応）
- 各音は基準音（音名選択）から見た移動ドの階名（ド・レ・ミ・ファ・ソ・ラ・シ）、基準「ド」から見た相対オクターブ、1拍内での単位数（`dur`）で指定する
- 実際の周波数は、プレビュー再生（5-2参照）と同じ`computePreviewBaseFreq`（「ー2」を「ー1」相当に補正した基準周波数）をもとに算出する。ピッチ検出・判定用の基準周波数（state.baseFreq）は変更しない
- 「長音階／短音階」切り替えボタンがONのとき（短音階選択時）は、度数が「ミ」の音（オクターブを問わず）はレ#相当、「ラ」の音（オクターブを問わず）はソ#相当の半音に置き換えて鳴らす。これは6-2で常設した「ミ短＝3」「ラ短＝8」のsemitone値をそのまま流用している
  - なお、ユーザー指示の文面では基準オクターブの「ラ」のみ明記されていたが、パターン4・7に登場する1オクターブ上の「ラ↑」にも度数名が同じという理由で同様に短音階置換を適用する仕様とした（オクターブに関わらず度数名で判定）

### 12-5. 視覚インジケーター
- 再生中、音名リング上で今鳴っている音の半音位置に対応する区画（短音階選択時はミ短・ラ短の区画も含む）に緑色の光の輪（`.ring-note.chant-target`）を表示する
- この光の輪は、マイクで検出した実際の声の色分け表示（`.active`／`.oct-base`等、6-3参照）とは別枠の表示であり、塗りつぶしではなく外側のリング状の光にすることで、両方を同時に見比べられるようにしている（＝再生中の目標音と、実際に唱えている声の音を並べて確認できる）

### 12-6. 全8パターンの内容
拍速はすべて45。各パターンの正確な音の並び（度数・オクターブ・単位数）は`script.js`の`CHANT_PATTERNS`配列に定義されている。

| パターン名 | 練習内容（1拍の分割） | 拍数（休止含む） |
|---|---|---|
| １・１ | 1拍1音（サブディビジョンなし）の基本形。ド〜ミ↑まで上行し休止を挟んで下行する対称形 | 17 |
| ２・２ | 1拍を2:2に分ける | 17 |
| ２・１・１ | 1拍を2:1:1に分ける | 14 |
| １・１・２ | 1拍を1:1:2に分ける | 15 |
| ３・１ | 1拍を3:1に分ける | 17 |
| １・３ | 1拍を1:3に分ける | 17 |
| １・１・１・１ | 1拍を1:1:1:1（4分割）に分ける。4種のフレーズを休止で区切って連続させる長めの構成 | 47 |
| １・２・１ | 1拍を1:2:1に分ける | 17 |

**データ転記に関する補足**：パターン「３・１」の5拍目は、ユーザー指示の原文が「ラーーと↑」だったが、他の全パターンで同じ構造の位置に一貫して「ど↑」（1オクターブ上のド）が使われていることから、「と↑」は「ど↑」の誤字と判断し補正して実装した。

## 13. 未確定事項

- 男女ワンタッチ切り替えボタンを正式に採用するか（5-3）
- 円形メーターの配色・アニメーション方針の最終調整
- タップ再生を「9区画それぞれ個別に確認できる」現行仕様のまま維持するか、他のコンダクターアプリのように「基準音のみ再生」に簡素化するか（保留中。現状は9区画個別プレビューを維持する方針）
